image: mcr.microsoft.com/dotnet/sdk:6.0

stages:
    - build
    - test

build:
    stage: build
    script:
        - "dotnet build Desu"
    artifacts:
      paths:
        - Desu/bin/

test:
    stage: test
    script: 
        - "dotnet test --verbosity normal"
